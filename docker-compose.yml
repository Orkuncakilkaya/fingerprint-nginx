services:
  web:
    build:
      context: .
      args:
        - DNS_RESOLVER=8.8.8.8
        - IDENTIFICATION_BASE=identification
        - AGENT_BASE=agent
    ports:
      - "8080:80"
    restart: unless-stopped
